import React from "react"
import { Icon } from "semantic-ui-react"

export default () => {
    return (
        <Icon link inverted name="arrow circle down" size="huge"
            style={{
                position: "absolute",
                bottom: "10%",
                left: "90%"
            }}/>
    )
}
